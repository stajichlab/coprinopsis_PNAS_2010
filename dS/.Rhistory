ccin<-read.table("dup_pair.tab",header=T,sep="\t")
genomedist<-read.table("genome_pair_config.dat",header=T,sep="\t")
genomedist<-read.table("genome_pair_config.dat",header=F,sep="\t")
summary(genomedist)
summary(ccin)
summary(ccin)
summary(genomedist)
HH <- subset(ccin,ccin$geneconfig == "HeadToHead")
HT <- subset(ccin,ccin$geneconfig == "HeadToTail")
TT <- subset(ccin,ccin$geneconfig == "TailToTail")
TT <- subset(ccin,ccin$geneconfig == "TailtoTail")
HT <- subset(ccin,ccin$geneconfig == "HeadtoTail")
HH <- subset(ccin,ccin$geneconfig == "HeadtoHead")
boxplot(t(HH$ds,HT$ds,TT$ds))
boxplot(c(HH$ds,HT$ds,TT$ds))
boxplot(HH$ds,HT$ds,TT$ds, names=c("HH","HT","TT"))
boxplot(HH$dn,HT$dn,TT$dn, names=c("HH","HT","TT"))
ks.test(HT$dn,TT$dn)
t.test(HT$dn,TT$dn)
t.test(HH$dn,HT$dn)
t.test(HH$ds,HT$ds)
ks.test(HH$ds,HT$ds)
ks.test(HH$ds,TT$ds)
ks.test(HT$ds,TT$ds)
q()
ls()
summary(ccin)
ccin$dNdS <- ccin$dN / ccin$dS
clean<-subset(ccin,ccin$dS != 'NA')
clean<-subset(ccin,ccin$dS != 'NA' & ccin$dN != 'NA')
clean$dNdS <- clean$dN / clean$dS
summary(clean$dNdS)
clean
summary(clean)
clean$dNdS <- (clean$dN / clean$dS)
clean$dN
clean
clean<-subset(ccin,ccin$dS ne 'NA' & ccin$dN ne 'NA')
clean<-subset(ccin,ccin$dS == 'NA')
clean
help("ne")
help("is")
ccin_clean <-read.table("Ccin_dsdn_clean.tab",header=T,sep="\t")
ccin_clean
ccin_clean$OMEGA = ccin_clean$dN/ccin_clean$dS
ccin_clean$OMEGA <- ccin_clean$dN/ccin_clean$dS
(ccin_clean$OMEGA) <- (ccin_clean$dN/ccin_clean$dS)
(ccin_clean$OMEGA) = (ccin_clean$dN/ccin_clean$dS)
OMEGA = (ccin_clean$dN/ccin_clean$dS)
summary(OMEGA)
OMEGA <- (ccin_clean$dN/ccin_clean$dS)
summary(OMEGA)
OMEGA <- ccin_clean$dN/ccin_clean$dS
summary(OMEGA)
OMEGA <- c(ccin_clean$dN/ccin_clean$dS)
OMEGA
OMEGA <- vector(ccin_clean$dN/ccin_clean$dS)
hist(ccin_clean$dN/ccin_clean$dS, 100)
ccin_clean$dN
ccin_clean$dn
OMEGA <- (ccin_clean$dn/ccin_clean$ds)
OMEGA
hist(OMEGA)
ccin_clean$OMEGA <- (ccin_clean$dn/ccin_clean$ds)
ccin_clean$OMEGA
ccin_clean
subset(ccin_clean,ccin_clean$OMEGA > 1)
subset(ccin_clean,ccin_clean$OMEGA > 1.1)
subset(ccin_clean,1/ccin_clean$dsdn > 1.1)
subset(ccin_clean,(1/ccin_clean$dsdn) > 1.1)
subset(ccin_clean,(1/ccin_clean$dsdn) > 1.2)
subset(ccin_clean,(1/ccin_clean$dsdn) > 1.4)
subset(ccin_clean,(1/ccin_clean$dsdn) > 1.1)
ccin_clean$OMEGA = 1/ccin_clean$dsdn
subset(ccin_clean,(1/ccin_clean$dsdn) > 1.1)
q()
me<-read.table("A2Bmutr_dups.exp",header=T,sep="\t")
summary(me)
plot(me$EXP_LEFT,me$EXP_RIGHT)
dup<-subset(me,me$DUPLICATED=="YES")
dup
plot(dup$EXP_LEFT,dup$EXP_RIGHT)
cor(dup$EXP_LEFT,dup$EXP_RIGHT)
cor.test(dup$EXP_LEFT,dup$EXP_RIGHT)
summary(dup)
ht<-subset(dup,dup$CONFIG == "HeadToTail")
cor.test(ht$EXP_LEFT,ht$EXP_RIGHT)
plot(ht$EXP_LEFT,ht$EXP_RIGHT)
ht
ht<-subset(dup,dup$CONFIG == "HeadtoTail")
plot(ht$EXP_LEFT,ht$EXP_RIGHT)
tt<-subset(dup,dup$CONFIG == "TailToTail")
plot(tt$EXP_LEFT,tt$EXP_RIGHT)
tt<-subset(dup,dup$CONFIG == "TailtoTail")
plot(tt$EXP_LEFT,tt$EXP_RIGHT)
tt_all <-subset(me,me$CONFIG = "TailtoTail")
tt_all <-subset(me,me$CONFIG == "TailtoTail")
summmary(tt_all)
summary(tt_all)
plot(tt_all$EXP_LEFT,tt_all$EXP_RIGHT)
cor(tt_all$EXP_LEFT,tt_all$EXP_RIGHT)
q()
ls()
cor(TT$model1_expression,TT$model2_expression)
cor(HT$model1_expression,HT$model2_expression)
q()
